package com.fa20se28.vma.enums;

public enum VehicleStatus {
    AVAILABLE,
    AVAILABLE_NO_DRIVER,
    ON_ROUTE,
    MAINTENANCE,
    PENDING_APPROVAL,
    DELETED,
    REJECTED,
    NEED_REPAIR,
    RETURNING
}
